"""
Config Package

This package contains configuration classes and utilities for defining
experiment parameters. The `ExperimentConfig` dataclass serves as the single
source of truth for all settings, enabling easy switching between different
forecasting tasks (one-step, multi-step) and ensuring reproducibility.
"""